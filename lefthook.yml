pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm lint
    format:
      glob: "*.{js,jsx,ts,tsx,json,css,md,yaml,yml}"
      run: pnpm format:check
    check-types:
      glob: "*.{ts,tsx}"
      run: pnpm check-types
