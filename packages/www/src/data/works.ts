import { metadata as chofusaiMetadata } from "../app/works/(pages)/chofusai/_metadata";
import { metadata as moMetadata } from "../app/works/(pages)/mo/_metadata";
// import { metadata as portfolioMetadata } from "../app/works/(pages)/portfolio/_metadata";
import { metadata as risaProMetadata } from "../app/works/(pages)/risa-pro/_metadata";
// import { metadata as shokujinWikiMetadata } from "../app/works/(pages)/shokujin-wiki/_metadata";
import { metadata as uec3dMapMetadata } from "../app/works/(pages)/uec-3d-map/_metadata";

export const works = [
  // portfolioMetadata,
  chofusaiMetadata,
  moMetadata,
  risaProMetadata,
  // shokujinWikiMetadata,
  uec3dMapMetadata,
];
